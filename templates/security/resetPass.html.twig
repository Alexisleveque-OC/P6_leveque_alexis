{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <a class="offset-2 col-8 text-center oi oi-account-login icon-form"></a>

        <div class="offset-2 col-8 text-center form_resetPass">
            {{ form_start(formReset) }}
            {{ form_row(formReset.password) }}
            <hr>
            <button type="submit" class="btn btn-outline-success">Enregistrer mon nouveau mot de passe</button>
            {{ form_end(formReset) }}
        </div>
    </div>

{% endblock %}

{% block title %}
    Nouveau mot de passe
{% endblock %}
